%tr
  %td.thumb= image_tag(projectile.image_path)
  %td
    = projectile.name
    = "(#{pluralize(projectile.cost, 'AdmadaBuck')})"
    %br
    = truncate(projectile.description, :length => 100)
  %td
    = link_to image_tag('icon_small_add.png'), members_armada_components_path(@armada, :resource_name => 'Projectile', :resource_id => projectile.id), :method => :post
    = link_to 'add to armada', members_armada_components_path(@armada, :resource_name => 'Projectile', :resource_id => projectile.id), :method => :post
