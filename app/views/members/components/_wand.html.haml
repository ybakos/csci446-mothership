%tr
  %td.thumb= image_tag(wand.image_path)
  %td
    = wand.name
    = "(#{pluralize(wand.cost, 'AdmadaBuck')})"
    %br
    = truncate(wand.description, :length => 100)
  %td
    = link_to image_tag('icon_small_add.png'), members_armada_components_path(@armada, :resource_name => 'Wand', :resource_id => wand.id), :method => :post
    = link_to 'add to armada', members_armada_components_path(@armada, :resource_name => 'Wand', :resource_id => wand.id), :method => :post
